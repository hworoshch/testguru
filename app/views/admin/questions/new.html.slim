h1 = t('.header', test: @test.title)
= form_for [:admin, @test, @question] do |f|
  = render 'errors', object: @question
  = f.hidden_field :test_id, value: @test.id
  p
    = f.label :body
    = f.text_area :body
  = f.submit