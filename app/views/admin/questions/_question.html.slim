tr
  td = link_to question.body, admin_question_path(question)
  td = link_to t('application.link.edit'), edit_admin_question_path(question)
  td = link_to t('application.link.delete'), admin_question_path(question), method: :delete, data: { confirm: t('application.confirm') }
